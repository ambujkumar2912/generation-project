import { FormEvent, useState } from 'react';
import { Link } from 'react-router-dom';
import { Avatar } from '../components/Avatar';
import { useAuth } from '../context/AuthContext';
import { mockComposerActions, mockHelpRequests, mockInterests, mockMoments, mockPrompts, mockTopics, type HomeMoment, type HomePost } from '../mock/home';
import { useGenerationFeed } from '../hooks/useGenerationFeed';


export function HomePage() {
  const { user, verifiedCohorts } = useAuth(); const cohort = verifiedCohorts.find((item) => item.is_primary) ?? verifiedCohorts[0];
  const feed = useGenerationFeed();
  if (!user) return null;
  return <main className="mx-auto max-w-[1280px] px-4 py-5 sm:px-6"><div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_300px]"><div className="min-w-0 space-y-5"><header><h1 className="font-display text-4xl font-semibold tracking-tight text-navy">Hey {user.display_name.split(' ')[0]}! <span aria-hidden>👋</span></h1><p className="mt-2 text-sm text-ink/65">Let’s see what’s happening in your world today.</p></header><Moments data={mockMoments}/><Composer name={user.display_name} actions={mockComposerActions} onSubmit={feed.submit}/><section><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="font-display text-xl font-semibold text-navy">What’s going on in {cohort?.birth_year ?? 'your generation'}?</h2><div className="flex rounded-xl bg-white p-1 text-xs shadow-sm ring-1 ring-navy/[0.06]"><button className="rounded-lg bg-[#a98cf4] px-3 py-1.5 font-semibold text-white">All</button><button disabled className="px-3 py-1.5 text-ink/50">Moments</button><button disabled className="px-3 py-1.5 text-ink/50">Questions</button></div></div>{feed.loading ? <div className="mt-4 h-56 animate-pulse rounded-2xl bg-white"/> : feed.posts.length ? <div className="mt-4 grid gap-4 md:grid-cols-2">{feed.posts.map((post, index) => <PostCard key={post.id} post={post} cohort={cohort?.label ?? 'Generation'} compact={index > 1}/>)}</div> : <div className="mt-4 rounded-2xl border border-dashed border-[#dcd6ff] bg-white p-8 text-center text-sm text-ink/55">Your generation’s story starts here. Be the first to share something.</div>}{feed.error && <p className="mt-3 text-xs text-ink/45">{feed.error}</p>}<div className="mt-4 grid gap-4 sm:grid-cols-2">{mockPrompts.map((prompt) => <MiniPrompt key={prompt.id} {...prompt}/>)}</div></section><section className="rounded-2xl bg-[#f4f0ff] px-5 py-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-display text-lg font-semibold text-[#5a47c7]">Explore what you love</p><p className="text-xs text-ink/55">Dive into topics, people and conversations that match your vibe.</p></div><div className="flex flex-wrap gap-2">{mockInterests.map((item) => <button disabled key={item} className="rounded-full bg-white px-3 py-2 text-xs font-semibold text-navy shadow-sm">{item}</button>)}</div></div></section></div><aside className="space-y-4"><GenerationCard year={cohort?.birth_year} /><section className="rounded-2xl border border-navy/[0.08] bg-[#fffaf0] p-4 shadow-sm"><p className="font-display text-base font-semibold text-navy">What’s happening now</p><div className="mt-3 space-y-3">{mockTopics.map((topic) => <button disabled key={topic.id} className="flex w-full items-center gap-3 text-left"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-white text-[#7054d4] shadow-sm">{topic.icon}</span><span className="flex-1 text-sm font-medium text-navy">{topic.title}<small className="mt-0.5 block font-normal text-ink/45">{topic.talkingCount} talking</small></span><span className="text-[#dab44a]">✦</span></button>)}</div><button disabled className="mt-4 text-xs font-semibold text-navy">See more topics&nbsp; →</button></section><section className="rounded-2xl bg-[#edf6ff] p-4 shadow-sm"><div className="flex items-center justify-between"><p className="font-display text-base font-semibold text-navy">People asking for help</p><button disabled className="text-xs font-semibold text-[#5b46cc]">See all</button></div>{mockHelpRequests.map((request) => <div key={request.id} className="mt-3 flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-navy text-[10px] text-gold">{request.authorInitial}</span><p className="min-w-0 flex-1 text-xs font-medium text-navy">{request.question}</p><span className="rounded-full border border-[#c5d8ff] bg-white px-2 py-1 text-[10px] text-[#496bc2]">{request.answerCount} answers</span></div>)}</section><section className="rounded-2xl border border-navy/[0.08] bg-white p-4 shadow-sm"><div className="flex items-center gap-3"><Avatar name={user.display_name} src={user.avatar_url} size="sm"/><div><p className="font-display font-semibold text-navy">{user.display_name}</p><span className="text-xs text-verified">{cohort?.label}</span></div></div><Link to="/profile" className="mt-3 inline-block text-sm font-semibold text-navy">View profile&nbsp; →</Link></section></aside></div></main>;
}
function Moments({ data }: { data: HomeMoment[] }) { return <section className="overflow-x-auto rounded-2xl border border-navy/[0.07] bg-[#fffdf9] p-3 shadow-[0_6px_20px_rgba(27,42,74,.035)]"><div className="flex min-w-max items-stretch gap-4"><button disabled className="flex w-20 flex-col items-center justify-center rounded-xl bg-[#f4edff] px-2 text-[#6748d8]"><span className="text-3xl">＋</span><span className="mt-2 text-xs font-semibold">Share<br/>a moment</span></button>{data.map((moment, index) => <article key={moment.id} className="w-20 text-center"><span className={`mx-auto flex h-14 w-14 items-center justify-center rounded-full border-2 border-white bg-gradient-to-br ${['from-[#a885f1] to-[#ffd273]', 'from-[#ef9d7b] to-[#7453cd]', 'from-[#efcf6a] to-[#7d60d5]', 'from-[#aa88f5] to-[#ec9a7c]', 'from-[#e8a1cf] to-[#7654bf]'][index]} p-1 shadow-sm`}><span className="flex h-full w-full items-center justify-center rounded-full bg-navy text-sm font-semibold text-gold">{moment.name[0]}</span></span><p className="mt-2 truncate text-xs font-semibold text-navy">{moment.name}</p><p className="mt-1 line-clamp-2 text-[10px] leading-4 text-ink/55">{moment.note}</p></article>)}<button disabled className="w-12 rounded-xl bg-[#f8f6ff] text-xs font-semibold text-navy/65">See<br/>all</button></div></section>; }
function Composer({ name, actions, onSubmit }: { name: string; actions: { id: string; icon: string; label: string }[]; onSubmit: (content: string) => Promise<unknown> }) {
  const [open, setOpen] = useState(false); const [content, setContent] = useState(''); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState<string | null>(null);
  async function submit(event: FormEvent) { event.preventDefault(); if (!content.trim()) return; setSubmitting(true); setError(null); try { await onSubmit(content); setContent(''); setOpen(false); } catch { setError('Unable to share your post. Please try again.'); } finally { setSubmitting(false); } }
  return <section className="overflow-hidden rounded-2xl border border-[#f1e7c9] bg-white shadow-sm"><form onSubmit={submit}><div className="flex items-center gap-3 bg-[#fffaf0] px-4 py-3"><Avatar name={name} size="sm"/>{open ? <textarea autoFocus value={content} onChange={(event) => setContent(event.target.value)} maxLength={2000} placeholder={`What’s on your mind, ${name.split(' ')[0]}?`} className="min-h-16 flex-1 resize-none bg-transparent text-sm text-ink outline-none placeholder:text-ink/55"/> : <button type="button" onClick={() => setOpen(true)} className="flex-1 text-left text-sm text-ink/65">What’s on your mind, {name.split(' ')[0]}?</button>}<button disabled type="button" className="rounded-full bg-white px-3 py-2 text-xs font-semibold text-[#d1721b] shadow-sm">☺ Feeling / Activity</button></div>{open && <div className="flex items-center justify-between border-b border-navy/5 px-4 py-2 text-xs text-ink/45"><span>{content.length}/2000</span><div className="flex gap-2"><button type="button" onClick={() => { setOpen(false); setContent(''); }} className="px-3 py-1.5 font-semibold text-ink/55">Cancel</button><button disabled={submitting || !content.trim()} className="rounded-lg bg-navy px-3 py-1.5 font-semibold text-paper disabled:opacity-50">{submitting ? 'Sharing…' : 'Share'}</button></div></div>}{error && <p className="px-4 pt-2 text-xs text-red-600">{error}</p>}</form><div className="grid grid-cols-5 gap-1 px-3 py-3">{actions.map((action) => <button disabled key={action.id} className="flex items-center justify-center gap-2 rounded-xl py-2 text-xs font-semibold text-navy hover:bg-[#f8f7ff]"><span className="text-lg text-[#8a62e8]">{action.icon}</span>{action.label}</button>)}</div></section>;
}
function PostCard({ post, cohort, compact = false }: { post: HomePost; cohort: string; compact?: boolean }) { return <article className="overflow-hidden rounded-2xl border border-navy/[0.08] bg-white shadow-[0_6px_20px_rgba(27,42,74,.035)]"><div className="p-4"><div className="flex items-center gap-2"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-navy text-xs font-semibold text-gold">{post.author[0]}</span><div className="min-w-0 flex-1"><p className="text-xs font-semibold text-navy">{post.author} <span className="ml-1 rounded-full bg-[#eee7ff] px-1.5 py-0.5 text-[9px] text-[#6948cf]">{cohort}</span></p><p className="text-[10px] text-ink/45">{post.time}</p></div><span className="text-ink/40">•••</span></div><p className="mt-3 text-sm leading-5 text-ink/80">{post.content}</p>{!compact && <div className="mt-3 flex flex-wrap gap-1">{post.tags.map((tag) => <span key={tag} className="rounded-full bg-[#f2effa] px-2 py-1 text-[10px] text-[#7159b8]">#{tag}</span>)}</div>}</div><div className={`${compact ? 'h-28' : 'h-32'} relative overflow-hidden bg-gradient-to-br ${post.tone}`}><div className="absolute inset-0 bg-[radial-gradient(circle_at_25%_25%,rgba(255,255,255,.6),transparent_10%),radial-gradient(circle_at_70%_65%,rgba(255,255,255,.35),transparent_18%)]"/></div><div className="flex items-center gap-5 px-4 py-3 text-xs text-ink/55"><span>♥ {post.reactions}</span><span>◯ {post.comments}</span><span>↗ 5</span><span className="ml-auto">♧</span></div></article>; }
function MiniPrompt({ text, label }: { text: string; label: string }) { return <article className="rounded-2xl border border-navy/[0.08] bg-white p-4 shadow-sm"><div className="flex items-center justify-between"><span className="text-xs font-semibold text-navy">Arjun P.</span><span className="rounded-full bg-[#fbe5ef] px-2 py-1 text-[10px] text-[#ce4b82]">{label}</span></div><p className="mt-3 text-sm leading-5 text-ink/75">{text}</p><div className="mt-4 text-xs text-ink/50">♥ 32 &nbsp; ◯ 41 &nbsp; ♧</div></article>; }
function GenerationCard({ year }: { year?: number }) { return <section className="relative overflow-hidden rounded-2xl bg-[linear-gradient(145deg,#4e37a2,#21366f)] p-5 text-paper shadow-[0_10px_24px_rgba(44,43,112,.22)]"><div className="absolute -right-8 top-8 h-28 w-28 rounded-full border border-[#a18eff]/40 bg-[#7055c8]/40"/><p className="relative font-display text-4xl text-[#ffe170]">{year ?? '—'}</p><p className="relative font-display text-2xl">Generation</p><p className="relative mt-3 max-w-[160px] text-sm leading-5 text-paper/85">Different lives. Same timeline. Let’s grow up together.</p><button disabled className="relative mt-4 rounded-full bg-[#ffe073] px-4 py-2.5 text-xs font-semibold text-navy">Explore your generation&nbsp; →</button></section>; }
