import { useState, type ReactNode } from 'react';
import { Link } from 'react-router-dom';
import { Avatar } from '../components/Avatar';
import { useAuth } from '../context/AuthContext';

export function ProfilePage() {
  const { user, verifiedCohorts } = useAuth(); const [tab, setTab] = useState('Overview');
  const cohort = verifiedCohorts.find((item) => item.is_primary) ?? verifiedCohorts[0]; if (!user) return null;
  const tabs = ['Overview', 'Posts', 'Saved', 'Activity']; const joined = new Date((user as any).created_at ?? Date.now()).toLocaleDateString(undefined, { month: 'short', year: 'numeric' });
  return <main className="mx-auto max-w-6xl px-4 py-5 sm:px-6"><section className="overflow-hidden rounded-[2rem] bg-white shadow-sm ring-1 ring-navy/5"><div className="h-36 bg-[radial-gradient(circle_at_15%_30%,#c8d5ff,transparent_28%),radial-gradient(circle_at_80%_30%,#fde6a8,transparent_25%),linear-gradient(120deg,#192b55,#314d8d)] sm:h-48"/><div className="relative px-5 pb-6 sm:px-8"><div className="-mt-16 flex flex-col gap-4 sm:-mt-14 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4"><div className="relative"><div className="rounded-full border-4 border-white"><Avatar name={user.display_name} src={user.avatar_url} /></div><Link to="/profile/edit" aria-label="Edit profile photo" className="absolute bottom-1 right-0 rounded-full bg-gold p-2 text-xs text-navy shadow-md">◉</Link></div><div className="pb-1"><h1 className="font-display text-3xl font-semibold text-navy">{user.display_name}</h1>{user.username && <p className="mt-1 text-sm font-medium text-navy/55">@{user.username}</p>}{cohort && <span className="mt-2 inline-flex rounded-full bg-verified/10 px-3 py-1 text-sm font-semibold text-verified">● {cohort.label}</span>}</div></div><Link to="/profile/edit" className="rounded-xl bg-navy px-4 py-2.5 text-center text-sm font-semibold text-paper transition hover:-translate-y-0.5 hover:bg-navy-light">Edit Profile</Link></div><p className="mt-5 max-w-2xl text-sm leading-6 text-ink/70">{user.bio || 'A new member of Generation, here to learn, grow, and connect.'}</p></div></section><section className="mt-5 grid gap-3 sm:grid-cols-4"><Stat label="Friends" value="0" note="Coming soon"/><Stat label="Member since" value={joined} note="Part of Generation"/><Stat label="Status" value="Active" note="Your account is active"/><Stat label="Account" value="Generation Member" note="Built for belonging"/></section><div className="mt-6 flex gap-1 overflow-x-auto border-b border-navy/10">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`whitespace-nowrap px-4 py-3 text-sm font-semibold transition ${tab === item ? 'border-b-2 border-gold text-navy' : 'text-ink/45 hover:text-navy'}`}>{item}</button>)}</div>{tab !== 'Overview' ? <section className="mt-6 rounded-[1.5rem] bg-white p-10 text-center shadow-sm"><p className="font-display text-xl text-navy">{tab} is coming soon</p><p className="mt-2 text-sm text-ink/55">We’re building this carefully, without turning Generation into a popularity contest.</p></section> : <section className="mt-6 grid gap-5 lg:grid-cols-[1.35fr_.85fr]"><div className="space-y-5"><Card title="About me"><p className="leading-7 text-ink/70">{user.bio || 'There’s more to share when you’re ready.'}</p></Card><Card title="Interests"><div className="flex flex-wrap gap-2">{user.interests?.length ? user.interests.map((item) => <span key={item} className="rounded-full bg-[#eef1ff] px-3 py-1.5 text-sm font-medium text-navy">{item}</span>) : <Link to="/profile/edit" className="text-sm font-medium text-navy underline">Add your interests</Link>}</div></Card><Card title="Recent activity"><p className="text-sm text-ink/55">Your generation story starts here. Activity will appear as you participate.</p></Card></div><div className="space-y-5"><section className="overflow-hidden rounded-[1.5rem] bg-navy p-6 text-paper shadow-sm"><p className="text-sm font-medium text-gold">YOUR IDENTITY</p><div className="mt-5 font-display text-5xl font-semibold">{cohort?.birth_year ?? '—'}</div><p className="font-display text-2xl">Generation</p><p className="mt-6 text-sm leading-6 text-paper/65">Your generation is your community.</p></section><Card title="Private information"><div className="space-y-3 text-sm"><p className="flex justify-between"><span>Date of birth</span><span className="font-medium text-ink/45">Private 🔒</span></p><p className="flex justify-between"><span>Phone number</span><span className="font-medium text-ink/45">Private 🔒</span></p><p className="border-t border-navy/10 pt-3 text-xs text-ink/50">Your private information stays private.</p></div></Card></div></section>}</main>;
}
function Stat({ label, value, note }: { label: string; value: string; note: string }) { const icons: Record<string, string> = { Friends: '♧', 'Member since': '▣', Status: '♛', Account: '◈' }; const tones: Record<string, string> = { Friends: 'bg-[#f0ecff] text-[#6652d8]', 'Member since': 'bg-[#eaf4ff] text-[#1976d2]', Status: 'bg-[#eff9ef] text-[#188149]', Account: 'bg-[#fff1e8] text-[#e36a21]' }; return <div className="flex items-center gap-3 rounded-2xl border border-navy/[0.08] bg-white p-4 shadow-[0_4px_15px_rgba(27,42,74,.035)]"><span className={`flex h-11 w-11 items-center justify-center rounded-full text-lg ${tones[label]}`}>{icons[label]}</span><div><p className="text-sm font-medium text-navy/75">{label}</p><p className="mt-0.5 font-display text-base font-semibold text-navy">{value}</p><p className="mt-0.5 text-xs text-ink/45">{note}</p></div></div>; }
function Card({ title, children }: { title: string; children: ReactNode }) { return <section className="rounded-2xl border border-navy/[0.08] bg-white p-5 shadow-[0_4px_15px_rgba(27,42,74,.035)]"><h2 className="font-display text-base font-semibold text-navy">{title}</h2><div className="mt-4">{children}</div></section>; }
