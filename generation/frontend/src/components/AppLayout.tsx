import { Link, NavLink, Outlet, useNavigate } from 'react-router-dom';
import { Avatar } from './Avatar';
import { useAuth } from '../context/AuthContext';

const future = ['Explore', 'My Generation', 'Saved', 'Settings', 'Help & Support'];
export function AppLayout() {
  const { user, logout } = useAuth(); const navigate = useNavigate();
  if (!user) return null;
  const link = ({ isActive }: { isActive: boolean }) => `flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition ${isActive ? 'bg-navy text-paper shadow-sm' : 'text-navy/65 hover:bg-navy/5 hover:text-navy'}`;
  return <div className="min-h-screen bg-[#fafbfe] text-ink"><aside className="fixed inset-y-0 left-0 hidden w-60 flex-col border-r border-navy/10 bg-white p-4 lg:flex"><Link to="/home" className="flex items-center gap-2 font-display text-2xl font-semibold text-navy"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-navy text-base text-gold">✦</span>Generation</Link><nav className="mt-8 space-y-1"><NavLink to="/home" className={link}>⌂ Home</NavLink>{future.slice(0, 3).map((item) => <span key={item} className="flex cursor-not-allowed items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-ink/35">○ {item}</span>)}<div className="my-3 border-t border-navy/10"/><NavLink to="/profile" className={link}>♙ Profile</NavLink><NavLink to="/profile/edit" className={link}>✎ Edit Profile</NavLink><span className="flex cursor-not-allowed items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-ink/35">⚙ Settings</span><div className="my-3 border-t border-navy/10"/><span className="flex cursor-not-allowed items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium text-ink/35">◉ Help & Support</span></nav><div className="mt-auto rounded-2xl border border-gold/30 bg-gold/5 p-4 text-center"><p className="text-xs text-navy/60">You’re part of</p><p className="mt-1 font-display text-lg text-navy">your Generation</p><p className="mt-2 text-xs leading-5 text-ink/55">Connect, share and grow together.</p></div><button onClick={() => { logout(); navigate('/'); }} className="mt-3 rounded-xl px-3 py-2 text-left text-sm font-medium text-red-600 hover:bg-red-50">⇥ Log out</button></aside><div className="lg:pl-60"><header className="sticky top-0 z-20 border-b border-navy/10 bg-white/95 px-5 py-2.5 backdrop-blur"><div className="mx-auto flex max-w-[1280px] items-center justify-between gap-4"><div className="relative hidden max-w-sm flex-1 md:block"><input disabled placeholder="⌕  Search in your generation..." className="w-full rounded-lg border border-navy/10 bg-[#fafbfe] px-4 py-2.5 text-sm placeholder:text-ink/45"/></div><div className="flex items-center gap-3 md:ml-auto"><button disabled title="Coming soon" className="rounded-xl p-2 text-lg text-navy/60">♧</button><button disabled title="Coming soon" className="rounded-xl p-2 text-lg text-navy/60">◌</button><Link to="/profile" className="flex items-center gap-2"><Avatar name={user.display_name} src={user.avatar_url} size="sm"/><span className="hidden text-sm font-semibold text-navy sm:block">{user.display_name}⌄</span></Link></div></div></header><Outlet/><footer className="px-5 pb-20 pt-6 text-center text-xs text-ink/45 lg:pb-5">© Generation · Made for your generation. <span className="ml-2">About · Privacy · Terms · Contact</span></footer></div><nav className="fixed inset-x-0 bottom-0 z-30 flex justify-around border-t border-navy/10 bg-white px-3 py-2 lg:hidden"><NavLink to="/home" className={link}>⌂</NavLink><span className="px-3 py-2 text-ink/30">○</span><NavLink to="/profile" className={link}>♙</NavLink><NavLink to="/profile/edit" className={link}>✎</NavLink></nav></div>;
}
